<%= image_tag @item.get_item_image(20,20) %>
            
<%= @item.name%>
            
<%= @item.introduction %>
            
<%= @item. price %>円
            
<%= form_with model: @cart_items, url: delivery_path(@cart_items.id), method: :patch do |f| %>
    <%= f.select :quantity, options_for_select(@item, {selected: cart.quantity.to_i} ) %>
<% end %>

<%= link_to 'カートに入れる', cart_items_path, class: "btn btn-secondary" %>