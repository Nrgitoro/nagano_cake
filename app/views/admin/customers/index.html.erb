<div class="container"> 
 <div class="row">
  <div class="col-md-8 offset-md-1">
  <p>会員一覧</p>
<table class="table table-striped">
      <tr>
      <th>会員ID</th>
      <th>氏名</th>
      <th>メールアドレス</th>
      <th>ステータス</th>
     </tr>
   <% @genres.each do |f| %>
      <tr>
        <td><%= f.id %></td>
        <td><%= f.name %></td>
        <td><%= f.name %></td>
        <td>ステータス</td>
      </tr>
 <% end %>
  </table>
  </div>
  </div>
  </div>