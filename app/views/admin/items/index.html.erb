<!DOCTYPE html>
<p id="notice"></p>
　<div calss="row">
    <div class="col-md-8 offset-md-1">
      <h2>商品一覧</h2>
      <table class="table  table-inverse">


        <thead>
          <tr>
            <th>商品ID</th>
            <th>商品名</th>
            <th>税別価格</th>
            <th>ジャンル</th>
            <th>ステータス</th>
          </tr>
        </thead>

            <% @items.each do |item| %>
        <tbody>
          <tr>
            <td><%= item.id %></td>
            <td><%= item.name %></p>
            <td><%= item.price %>円</td>
            <td></td>
            <td>
                <%= item.item_status ? '販売停止中' : '販売中' %></td>
          </tr>
        </tbody>
            <% end %>
<%= paginate @items %>
      </table>
    </div>
  </div>