<!DOCTYPE html>
<p id="notice"></p>
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>商品編集</h2>

      <%= form_with model: @item, url: admin_item_path(@item.id), method: :patch do |item|  %>
      <tbody>
        <tr>
          <th>
            <h5>商品画像
            　<%= item.file_field :item_image, accept: "image/*" %>
            </h5>
          </th>
          <th>
            <h5>商品名
            　　<%= item.text_field :name, class:"w-75" %>
            </h5>
          </th>
          <th>
            <h5>商品説明
            　<%= item.text_field :introduction, class:"w-75" %>
            </h5>
          </th>
          
          <th>
            <h5>ジャンル
            　<%= item.collection_select :genre_id, Genre.all, :id, :name, {include_blank: "---"}, class:"w-75" %>
            </h5>
          </th>
          
          <th>
            <h5>税別価格
            　<%= item.text_field :price, class:"w-75" %>円
            </h5>
          </th>

          <%= item.submit '変更を保存', class:"btn btn-success" %>
      <% end %>
      </tbody>
    </div>
  </div>
</div>